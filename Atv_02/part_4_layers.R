# Instructions
# Now we are going to add layers and the corresponding aesthetic mappings. 
# For the murders data, we plotted total murders versus population sizes in the videos.

# Write code to explore the murders data frame to remind yourself of the two variables 
# (total murders and population size) that we want to plot.

library(dplyr)
library(ggplot2)
library(dslabs)

#exibe os nomes das variáveis do dataset murders
names(murders)
